# 📚 Работы по AJAX и JSON

Этот репозиторий содержит три выполненных задания по работе с AJAX, JSON и динамической загрузкой данных.

---

## Задание 1: AJAX поиск с ленивой загрузкой

### Описание
Простая система поиска с динамической подгрузкой результатов через AJAX.

### Возможности
-  Поиск по текстовому запросу
-  Загрузка результатов порциями по 10 элементов
-  Кнопка "Загрузить ещё" для дозагрузки
-  Ленивая загрузка (lazy loading)
-  Отображение "Результат №1", "Результат №2" и т.д.

### Файлы
```
Lab6_AJAX_1/
├── index.html      - Главная страница
├── style.css       - Стили
├── main.js         - JavaScript логика
└── AJAX.php        - Серверная обработка
```

### Как работает
1. Пользователь вводит запрос и нажимает "Поиск"
2. JavaScript отправляет POST запрос к AJAX.php
3. PHP возвращает первые 10 результатов в JSON
4. JavaScript отображает результаты на странице
5. При клике "Загрузить ещё" подгружаются следующие 10 результатов
6. Новые результаты добавляются к уже существующим

---

## Задание 2: Система новостей с пагинацией

### Описание
Полноценная система новостей с постраничной навигацией, поиском и модальными окнами.

### Возможности
-  Автоматическая загрузка новостей при входе
-  По 5 новостей на странице
-  Пагинация без перезагрузки (Prev | 1 | 2 | 3 | Next)
-  Модальное окно с полным текстом новости
-  Поиск по новостям
-  Заголовки "Новость №1", "Новость №2"

### Файлы
```
Lab6_AJAX_2/
├── index.html  - Главная страница
├── style.css   - Стили
├── main.js     - JavaScript логика
└── ajax.php    - Серверная обработка
```

### Как работает
1. При загрузке страницы автоматически запрашиваются первые 5 новостей
2. Отображается текущая страница с новостями
3. Внизу страницы кнопки пагинации
4. При клике на номер страницы:
   - JavaScript отправляет запрос с номером страницы
   - PHP возвращает нужные 5 новостей
   - Старые новости **заменяются** новыми (не добавляются)
5. При клике "More info":
   - Открывается модальное окно
   - JavaScript запрашивает полный текст по ID новости
   - Отображается детальная информация

---

## 📅 Задание 3: Расписание занятий

### Описание
Интерактивное расписание занятий с фильтрами, cookies и аккордеоном.

### Возможности
-  Автозагрузка расписания на текущий день
-  Автоматическое определение даты и недели
-  Отображение верхней/нижней недели
-  Tooltip с номером недели при наведении
-  Фильтрация по предмету, курсу, группе, дню
-  Сохранение фильтров в cookies
-  Зелёная подсветка загруженных фильтров
-  Аккордеон с детальной информацией о паре

### Файлы
```
Lab6_AJAX_3/
├── index.html       - Главная страница
├── style.css        - Стили
├── main.js          - JavaScript логика
├── schedule.json    - Расписание пар (3 курс)
└── bells.json       - Расписание звонков
```

### Как работает

#### Загрузка данных
1. При открытии страницы загружаются JSON файлы
2. Определяется текущая дата и день недели
3. Вычисляется номер недели с 1 сентября 2025 года
4. Определяется тип недели (верхняя/нижняя)
5. Автоматически устанавливается фильтр на текущий день

#### Фильтрация
Фильтры работают одновременно (логическое И):
- **Поиск** - по названию предмета
- **Курс** - выбор курса (1-4)
- **Группа** - выбор группы (ИВТ-1 до ИВТ-5)
- **День** - день недели (Пн-Сб)

Также учитывается тип недели:
- Если пара только на верхней неделе (`"week": "upper"`)
- Если пара только на нижней неделе (`"week": "lower"`)
- Если пара каждую неделю (`"week": "both"`)

#### Cookies
При изменении любого фильтра:
1. Значения сохраняются в cookies на 30 дней
2. При следующем заходе фильтры восстанавливаются
3. Восстановленные поля подсвечиваются зелёным на 2 секунды

#### Аккордеон
При клике на элемент расписания:
1. Раскрывается блок с детальной информацией
2. Все остальные блоки автоматически закрываются
3. Остальные элементы плавно смещаются вниз
4. Кнопка "ЕЩЁ" меняет цвет с зелёного на красный

### Структура JSON

#### schedule.json
```json
{
  "schedule": [
    {
      "id": 1,
      "day": "Понедельник",
      "dayNumber": 1,
      "group": "ИВТ-3",
      "course": 3,
      "pairNumber": 2,
      "subject": "Операционные системы",
      "type": "лаб",
      "teacher": "ст. преп. Котенко Ю.В.",
      "room": "413",
      "week": "both"
    }
  ]
}
```

#### bells.json
```json
{
  "bells": [
    {
      "number": 1,
      "start": "8:00",
      "end": "9:30"
    }
  ]
}
```

### Логика определения недели
```javascript
// начало отсчёта - 1 сентября 2025 года
const startDate = new Date(2025, 8, 1);

// вычисляем разницу в днях
const diffDays = Math.floor((currentDate - startDate) / (1000 * 60 * 60 * 24));

// вычисляем номер недели
const weekNumber = Math.floor(diffDays / 7) + 1;

// определяем тип
const weekType = weekNumber % 2 === 1 ? 'верхняя' : 'нижняя';
```

### Технологии
- HTML5 + CSS3
- Vanilla JavaScript
- JSON (без PHP)
- Cookies API

---

## Технические детали

### AJAX запросы

**Задание 1 (POST):**
```javascript
fetch('AJAX.php', {
  method: 'POST',
  body: formData  // query, offset, limit
})
```

**Задание 2 (POST):**
```javascript
fetch('news-ajax.php', {
  method: 'POST',
  body: formData  // page, limit, search
})
```

**Задание 3 (GET):**
```javascript
fetch('schedule.json')
  .then(response => response.json())
```

### Формат ответов

Все PHP скрипты возвращают JSON:
```json
{
  "success": true,
  "data": [...],
  "hasMore": true
}
```

---
